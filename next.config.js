/** @type {import('next').NextConfig} */
const nextConfig = {};

/* module.exports = {
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: 'https://app.predictifsports.com/api/:path*',
      },
    ];
  },
};
 */

module.exports = nextConfig;
